package pages

import "dankmuzikk/views/layouts"

templ Playlists(isMobile bool) {
	@layouts.Default(isMobile, playlists())
}

templ playlists() {
	<main class={ "w-full", "h-full" }>
		<pre>
			Playlists:
		</pre>
	</main>
}
