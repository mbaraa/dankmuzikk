package pages

import "dankmuzikk/views/layouts"

templ Playlists(isMobile bool, themeName string) {
	@layouts.Default(isMobile, themeName, playlists())
}

templ playlists() {
	<main class={ "w-full", "h-full" }>
		<pre>
			Playlists:
		</pre>
	</main>
}
