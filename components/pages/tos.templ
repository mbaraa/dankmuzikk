package pages

import "dankmuzikk/components/layouts"

templ TOS(isMobile bool) {
	@layouts.Default(isMobile, tos())
}

templ tos() {
	<main class={ "w-full", "h-full" }>
		<pre>
			Terms of Service:
		</pre>
	</main>
}
